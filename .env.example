# ── Database ───────────────────────────────────────────────
DATABASE_URL=postgresql://fct_user:changeme@localhost:5432/federal_candidates
PGHOST=localhost
PGPORT=5432
PGUSER=fct_user
PGPASSWORD=changeme
PGDATABASE=federal_candidates

# ── Server ─────────────────────────────────────────────────
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
LOG_LEVEL=debug

# ── Authentication ─────────────────────────────────────────
JWT_SECRET=replace-with-secure-random-string-min-32-chars
JWT_EXPIRES_IN=7d

# ── FEC API ────────────────────────────────────────────────
FEC_API_KEY=DEMO_KEY
FEC_API_BASE=https://api.open.fec.gov/v1
FEC_RATE_LIMIT_MS=1100

# ── Data Collection ────────────────────────────────────────
COLLECTION_CONCURRENCY=2
BALLOTPEDIA_DELAY_MS=2000
